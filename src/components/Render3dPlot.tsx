"use client";

import React from "react";
import { DialogFormSchema } from "./ui/form/SpreadsheetDialog";

export default function Render3dPlot({
  fileMetadata,
  spreadsheet,
}: {
  fileMetadata: DialogFormSchema;
  spreadsheet: File;
}) {
  return <div>Render3dPlot</div>;
}
